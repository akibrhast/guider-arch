version: '3.8'

services:
  guider-arch:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: guider-arch
    ports:
      - "${PORT:-8090}:80"  # Default 8090, override with environment variable
    restart: unless-stopped
    labels:
      - "com.docker.compose.project=guider-arch"
