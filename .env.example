# Port Configuration for GUIDER Architecture
# Change this to an unused port on your Synology
PORT=8090

# To find available ports on your Synology, SSH in and run:
# sudo netstat -tulpn | grep LISTEN | sort -n -k4
